# 隐私说明 / Privacy Notice

> 适用范围：**在线部署版本**（含 `functions/_middleware.js` 访问统计的 Cloudflare Pages 站点）。
> 仅本地运行（双击 `字帖生成器.html`、`npm run dev`、或本地静态服务器）时**不产生任何网络上报**。
>
> 最近更新：v3.0.5（2026-09-18）

---

## 一、本地使用时

本应用是纯前端应用。您的汉字输入、字帖内容、字体文件、API Key 全部保存在**您自己浏览器的 localStorage** 中，不会上传到本项目的任何服务器。

若您配置了第三方大模型 API Key（DeepSeek / Agnes / ModelScope 等），应用会**从您的浏览器直接**调用该服务商的官方接口。此时您的请求内容受**该服务商**的隐私政策约束，与本项目无关。API Key 不会经过本项目的服务器。

---

## 二、在线版本会收集什么

在线版本为统计访问量，会在您访问时记录以下**汇总性**信息：

| 记录项 | 说明 |
|---|---|
| 假名化访客标识 | **带密钥的 SHA-256 哈希**（取前 128 位）。用于统计独立访客数，**不可反推 IP** |
| 访问时间 | 精确到秒 |
| 访问路径与 HTTP 方法 | 例如 `/` |
| 来源页（Referer） | 您是从哪个页面跳转过来的 |
| 国家 / 地区 | 由 Cloudflare 提供的粗粒度地理信息 |
| 设备类型 / 操作系统 / 浏览器 | 由 User-Agent **解析后的结果**（如 `desktop` / `Windows` / `Chrome`） |
| 语言偏好（Accept-Language） | 例如 `zh-CN` |
| 机房 / VPN 标记 | 是否来自已知数据中心网段（用于剔除爬虫与刷量） |

### v3.0.5 起**不再收集**

| 已移除 | 原因 |
|---|---|
| **原始 IP 地址** | 属于个人信息，且独立访客统计只需假名化标识即可完成 |
| **完整 User-Agent 字符串** | 指纹识别能力强，且统计报表从未使用（只用其解析结果） |
| **城市（city）** | 最精确的地理标识，且统计报表从未使用 |

> ⚠️ 哈希必须带密钥。IPv4 只有 2³² 个取值，**无盐哈希可在秒级被暴力反查**，等于没有脱敏。
> 密钥取自部署环境的 `IP_HASH_SALT`（未配置时回退到 `CRON_SECRET`）；两者都未配置时**不记录任何 IP 派生值**。

---

## 三、用途与保留

- **用途**：仅用于了解站点访问量、访问来源与设备构成，以便改进产品。不用于广告、不用于用户画像、不与第三方共享、不做跨站追踪。
- **保留期**：明细记录保留在部署者的 Cloudflare D1 数据库中，由部署者自行管理与清理。
- **无 Cookie 追踪**：站点本身不设置任何追踪 Cookie。唯一的 Cookie 是 `?admin=1` 用于让管理员把自己排除在统计之外。

---

## 四、如何退出统计

- **普通访客**：使用浏览器扩展或广告拦截器屏蔽 `/api/*` 与页面请求即可；本应用的全部功能**不依赖**统计接口。
- **站点管理员**：访问任意页面时带上 `?admin=1`，会写入一个排除 Cookie，此后您的访问不再被记录。

---

## 五、数据处理者

在线站点由项目维护者部署在 Cloudflare Pages 上，数据存储于其 Cloudflare D1 数据库。
如需查询或删除与您相关的记录，请通过 GitHub Issues 联系仓库维护者。

---

## 六、自托管提示

若您 fork 并自行部署本项目：

1. 请按 [analytics/README.md](./analytics/README.md) 配置 `CRON_SECRET`（**必须**，代码中已无默认密钥）与 `IP_HASH_SALT`。
2. 请根据您所在司法辖区的要求，自行补充隐私声明与告知同意流程。
3. 请勿把 `wrangler.toml`、`puppeteer-fonts.local.json` 等含个人配置的文件提交到公开仓库（已在 `.gitignore` 中排除）。
